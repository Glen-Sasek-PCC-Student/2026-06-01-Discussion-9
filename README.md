For this discussion post, you will use AI as a coding assistant to help you design an algorithm and write a complete program. You must guide the AI by limiting it to ONLY use the programming constructs we have covered so far in this course. With the help of AI: add comments to your program, improve user experience and debug your program.

Write a program that includes two functions with the same name, reverseDigit, utilizing function overloading. The first version of reverseDigit should take a single integer as an input parameter and return the number with its digits reversed. For instance, if the input is 12345, the function should return 54321. The second version should take two integer values as reference parameters and print the reversed values in the format: "The value of reverseDigit(5600, 7008) is 65, 8007". 

Please use the following function prototypes for your program:
```
int reverseDigit(int num1);
void reverseDigit(int &num2, int &num3);
```